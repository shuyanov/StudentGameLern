import React from "react";
import { FlexContainer } from "../../Common";
import styles from './HeaderBarFooter.module.scss'

export const HeaderBarFooter = () => {
    return (
        <div className={styles.HeaderBarFooter} />
    );
}
